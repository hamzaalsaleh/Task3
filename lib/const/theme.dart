import 'package:flutter/material.dart';

class Consts {
  static final Color backgroundDark = Colors.grey.shade800;
  static const Color indigoColor = Colors.indigo;
  static const SizedBox customSizedBox = SizedBox(width: 10, height: 10);
  static const Divider di = Divider(
    thickness: 0.8,
    color: Colors.grey,
    height: 1,
  );
}
